import 'package:flutter/material.dart';

class AirplayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Airplay app bar'),),
      body: Center(child: Text("airplay"),),
    );
  }
}
